# Synthetic record fixtures

This directory contains fictional records used by the test suite. They are
overlaid into temporary test repositories by `tests/test_cli_validation.py` and
`tests/test_github_view.py`.

These records are not part of the canonical Finance OSINT database. Public
examples should use `examples/corpus/` or real canonical records under
`records/`.
