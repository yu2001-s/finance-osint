# Example Corpora

Example corpora are separate from canonical data.

Normal commands run from the repository root do not load records under
`examples/corpus/`. These files are fixtures for learning, agent training, and
scratch testing.

To try a corpus without polluting the working tree, copy this repository to a
temporary directory, copy the corpus `records/` tree into that temporary clone,
then run the normal `fo` commands there.

